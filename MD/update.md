## 업데이트 내역 (v1.5.1)
- 다운로드 방식을 비동기 방식에서 스레드 방식으로 변경
- 원문 다운로드 옵션 추가
    - 환경 설정>고급 옵션>원문 제목으로 파일 저장
- 다운로드 백업 추가
    - 다운로드 시 백업 파일을 생성하며 다시 다운 시 백업 파일이 사용됨.
    - 경로는 환경폴더/list/작품원제목
    - ai 번역 및 epub 변환 지원

## 업데이트 내역 (v1.5.0)
- 사이트 지원 추가
    * 녹턴 지원 추가
    * 미드나이트 지원 추가
- 나로우 사이트 이미지 지원 추가
    * 나로우 사이트(소설가가 되자, 녹턴, 미드나이트)에서 이미지 포함하도록 변경
    * 경로는 환경 폴더에서 img 파일에 저장
    * epub 변환 시 epub에 포함
    * ai 번역 지원
- 프로세스 문제 해결
    * 기존 장시간 사용 후 종료 시 프로세스에 남아 있는 문제를 해결
- 검사 창 변경
    * 기존 번역에서 분분 번역 시행 시 생기던 단어의 설명 문제를 해결
    * 검사에서 반복되는 단어를 찾아 한번에 변경하는 검사 `용어집` 기능을 추가 
    * 기존 파파고 번역 시 일본어가 없어도 번역되던 문제를 해결
- 503 대응 추가
    * 다운로드 후 0.1초 대기 추가

## 업데이트 내역 (v1.4.2)
- 검사 창 변경
    * 기존 검사 창에서 배경에 그라데이션이 적용되는 문제 해결
    * 검사 검출 방법 변경
        * 검사 탭에서 `비율 모드`와 `글자 수 모드` 중 선택하여 선택 가능
        * 검사 탭에서 `비율` 또는 `글자 수`를 조정 가능
    * 내부 번역 추기
        * 검사 후 창에서 번역 버튼으로 파파고 번역 가능
        * 번역 시 장음 부호들이 사라짐
        * 일본어(한국어) 시 일본어를 한국어로 자동 대체
        * 한국어(일본어) 시 일본어 삭제
    * 검사 규칙 변경
        * 일본어 말고도 일본식 한자도 검출 범위에 포함
- ai 번역 창 탭 변경
    * 기존 탭에서 배경에 그라데이션이 들어가는 문제 해결
    * 긴 제목의 파일을 검사 시 생기던 창 크기 변동 문제 해결
- 검열 알고리즘 변경
    * 검열 시 검열을 더 쌔게 잡습니다.
    * ai 모델들을 각각 검열 여부를 정할 수 있습니다.
        * 이는 반환이 거부 시 하던 검열을 하지 않고 바로 청크를 분할 합니다.

## 업데이트 내역 (v1.4.1)
- c언어 dll 파일로 인해 검증 에러가 발생 했습니다.
    * 원래 이를 이용하여 빠른 검출을 시행하였는데, 이를 인증하지 않고 넣다보니 오검사 비율이 높아졌습니다.
    * 그래서 c언어를 제거하고 파이썬만을 이용하여 다시 제작 했습니다.
    * 따라서 원래 검출 속도 보다 10 ~ 100배 까지 시간이 걸릴 수 있다는 사실을 양해 바랍니다.
- 나머지 변경 사항은 없습니다. 
    * v1.4.0 업데이트 내역을 참고 바랍니다.

## 업데이트 내역 (v1.4.0)
- 기본 일본어 감지 비율 변경 (더 널널하게 검사)
- ai 번역 창 개편
    * 기존 모습과 달리 좌우 패널과 Tab으로 번경
    * 여러 모델 동시 번역 기능 추가
        * 여러 모델을 동시에 사용하여 번역을 보다 빠르게 진행 할 수 있음.
    * 프리셋 기능 추가
        * 번역 설정을 프리셋으로 저장하여 불러올 수 있음.
        * 이에 따라 번역 설정을 수동으로 저장하기를 눌러주어야 함.
    * 용어집 활성/비활성 추가
    * 일본어 잔존 검사 추가
        * 번역 후 남아있는 일본어를 감지하여 수동으로 고쳐 줄 수 있는 기능.
        * json 파일을 선택 후 사용
        * 검사 및 변환 후 다시 번역 시 반영
- json 파일 api 변경
    * 기존 json 파일은 저장시 번역 청크만 저장 했지만 이젠 원본 청크도 같이 저장

## 업데이트 내역 (v1.3.4)
- Gemma 모델 지원 추가
- 번역 중단 기능 추가
    * 기존에 아무 조건 없이 끌 수 있었던 창을 앞으론 완전히 종료 되어야만 끌 수 있게 변경
    * 번역 중단 시 epub이 생성 되지 않음.
- ai 번역에 `분할로 시작` 옵션 추가
    * 로직 상 청크 크기가 같아야 동일 파일로 인정되기 때문에 이를 추가
    * 옵션 값을 1로 올리면 2분할, 2이면 4분할, 3이면 8분할, 4이면 16분할
    * 기존 청크를 바꾸지 못하는 문제를 대체하여 해결
- 용어집 추출 알고리즘 변경
    * 기존 특정 구역에서 계속 머물며 멈추는 문제 해결
- 청크 선택을 최소 500까지 되도록 변경

## 업데이트 내역 (v1.3.3)
- ai 번역 청크를 최대 30000까지 되도록 변경
- 기존 스레드 종료가 되지 않는 문제 해결
- 용어집 관리 메뉴 추가
- 업데이트 후 sha 검증 추가

## 업데이트 내역 (v1.3.2)
- upx로 인한 바이러스 오검사라 판단하여 upx 제외한 빌드

## 업데이트 내역 (v1.3.1)
- `용어집` 변경 내역
    * 전체 삭제 기능 추가
    * 기존 글자 입력 방식에서 리스트 형식으로 변경
- `ai 용어집 추가` 기능을 변경
    * 기존 비율 계산 방식에서 c기반 새로운 규칙형 계산 방식으로 변경
    * 추출 시 프롬포트를 변경하여 일반 명사 추출 제외로 변경
    * 2차 검증으로 필요 없는 단어 제외 기능 추가
    * 삭제 버튼 색 조정
- `ai 용어집 추가` 기능 버그 수정
    * 기존 리미터 제한이 없던 버그를 수정
    * 기존 번역기 설정과 연동되지 않던 문제 해결
    * 모델이 고정되어 있던 문제 해결
- 업데이트 내역을 이전 내역까지 볼 수 있게 변경

## 업데이트 내역 (v1.3.0)
- `작품별 용어집` 추가
    * 기존 ai 번역에서 작품 선택 후 용어집 설정에서 작품별 용어집을 선택 가능
    * 용어집은 각 작품별로 별도로 존재
    * `ai 용어집 추가`는 ai를 사용하여 자동으로 용어집을 추출하는 기능
- `고급 옵션`에 사용자 지정 ai 프롬포트를 지정 가능.
- 라이트 모드에서 `남은 화수 있음` 버튼이 강조색에 의해 보이지 않던 문제 해결
- RAW ai 번역 시 파일 저장 이름이 기존 규칙과 다른 문제 해결

## 업데이트 내역 (v1.2.1)
- 기존 라이트 모드 시 ai 번역기 로그 내역이 잘 보이지 않는 문제 해결
- 고급 옵션 내부에 버전과 `의견 보내기`, `릴리스`, `GitHub` 홈페이지 연결 링크 등록
- RAW 번역 시 생기던 줄바꿈 문제와 구분선 문제 해결
- ai 번역기 ai 조건 비율을 글자 수에 따른 가변 조절 기능 추가
- ai 번역기 로그 내용 변경
- `원문 다운로드` 설명을 구체적으로 변경
- 프로그램 이름에 버전 표시
- 로드 창 디자인 변경

## 업데이트 내역 (v1.2.0)
- 작품 상세 보기 디자인 수정
- 주소 추가 시 주소 자동 변경 추가
    * 카쿠요무: 뒤에 '/'가 붙어 있으면 자동 삭제
    * 나로우: 뒤에 '/'가 없으면 자동 추가
- 목록에 아무것도 존재하지 않을 시
    * 처음 오셨나요?
    <br/> URL을 입력하여 직접 추가하시거나
    <br/> 작품을 검색해서 추가하여 당신만의 목록을 만드세요!
    * 라는 메세지 패널 추가
- `원문 다운로드` 옵션 추가
    * 다운로드 시 사이트 그대로 다운로드 되며 번역 및 EPUB 변환 로직이 변경됩니다.
    * ai 번역 시 번역 txt 파일이 같이 나오게 변경. 경로 : 환경 폴더/epub/raw_txt
    * 옵션 위치 : 환경 설정 -> 고급 옵션 -> 원문 다운로드
- 로드 창 변경
    * 기존 다운로드 방식을 새롭게 변경
    * 최신 릴리스 발견 시 업데이트를 묻게 변경
    * 업데이트를 넘어갈 수 있게 변경
- ai 번역 로그 창 스크롤 문제 해결

## 업데이트 내역 (v1.1.2)
- 테마 변경
    * 테마 색상을 보다 자연스럽게 변경
    * 그라데이션 방향을 조정하여 부드러움 수정
    * 기존 라이트 모드의 어둔운 느낌 제거
- 작품 상세보기에서 생긴 url 컬러가 변경되지 않는 문제 해결
- ai 번역기 내부 api 관련 로직 변경
    * ai 번역기 api 입력 부분을 복사/붙여넣기/전체선택만 되도록 변경
    * ai 번역기에서 기존의 api 삽입 시 바로 반영되지 않는 문제 해결
    * ai 번역기에서 api 삽입 시 자동 모델 검색 기능 추가
- 메인 아이콘 변경
- 프로그램 이름 변경
    * '다운로더' -> 'MINE DOWNLOADER - Novel(Syosetu, Kakuyomu) Downloader & Translator'
- 로드 파트 닫기 버튼 추가
- 시스템 관련
    * 기존에 폴더를 정의 하지 않을 시 생기던 폴더 문제 예외 처리
    * epub 변환시 아웃 폴더를 '환경 폴더'에서 '환경 폴더/epub'으로 변경

## 업데이트 내역 (v1.1.1)
- 태그 중복 문제 해결
- 업데이트 내역 뷰어 추가
- 인터넷 연결 확인 추가

## 업데이트 내역 (v1.1.0)
- 나로우 포인트 표시 문제 해결
- 기존 카쿠요무 파인더에서 추출 태그가 감지되지 않는 문제 해결
- 기존 추출 테그에 `서적 & 미디어화` 테그 추가
- 어플 아이콘 추가
- 자동 최신 버전 다운로드 기능 추가