## 업데이트 내역 (v1.3.1)
- `용어집` 변경 내역
    * 전체 삭제 기능 추가
    * 기존 글자 입력 방식에서 리스트 형식으로 변경
- `ai 용어집 추가` 기능을 변경
    * 기존 비율 계산 방식에서 새로운 규칙형 계산 방식으로 변경
    * 추출 시 프롬포트를 변경하여 일반 명사 추출 제외로 변경
    * 2차 검증으로 필요 없는 단어 제외 기능 추가
    * 삭제 버튼 색 조정
- `ai 용어집 추가` 기능 버그 수정
    * 기존 리미터 제한이 없던 버그를 수정
    * 기존 번역기 설정과 연동되지 않던 문제 해결
    * 모델이 고정되어 있던 문제 해결
- 업데이트 내역을 이전 내역까지 볼 수 있게 변경

## 업데이트 내역 (v1.3.0)
- `작품별 용어집` 추가
    * 기존 ai 번역에서 작품 선택 후 용어집 설정에서 작품별 용어집을 선택 가능
    * 용어집은 각 작품별로 별도로 존재
    * `ai 용어집 추가`는 ai를 사용하여 자동으로 용어집을 추출하는 기능
- `고급 옵션`에 사용자 지정 ai 프롬포트를 지정 가능.
- 라이트 모드에서 `남은 화수 있음` 버튼이 강조색에 의해 보이지 않던 문제 해결
- RAW ai 번역 시 파일 저장 이름이 기존 규칙과 다른 문제 해결

## 업데이트 내역 (v1.2.1)
- 기존 라이트 모드 시 ai 번역기 로그 내역이 잘 보이지 않는 문제 해결
- 고급 옵션 내부에 버전과 `의견 보내기`, `릴리스`, `GitHub` 홈페이지 연결 링크 등록
- RAW 번역 시 생기던 줄바꿈 문제와 구분선 문제 해결
- ai 번역기 ai 조건 비율을 글자 수에 따른 가변 조절 기능 추가
- ai 번역기 로그 내용 변경
- `원문 다운로드` 설명을 구체적으로 변경
- 프로그램 이름에 버전 표시
- 로드 창 디자인 변경

## 업데이트 내역 (v1.2.0)
- 작품 상세 보기 디자인 수정
- 주소 추가 시 주소 자동 변경 추가
    * 카쿠요무: 뒤에 '/'가 붙어 있으면 자동 삭제
    * 나로우: 뒤에 '/'가 없으면 자동 추가
- 목록에 아무것도 존재하지 않을 시
    * 처음 오셨나요?
    <br/> URL을 입력하여 직접 추가하시거나
    <br/> 작품을 검색해서 추가하여 당신만의 목록을 만드세요!
    * 라는 메세지 패널 추가
- `원문 다운로드` 옵션 추가
    * 다운로드 시 사이트 그대로 다운로드 되며 번역 및 EPUB 변환 로직이 변경됩니다.
    * ai 번역 시 번역 txt 파일이 같이 나오게 변경. 경로 : 환경 폴더/epub/raw_txt
    * 옵션 위치 : 환경 설정 -> 고급 옵션 -> 원문 다운로드
- 로드 창 변경
    * 기존 다운로드 방식을 새롭게 변경
    * 최신 릴리스 발견 시 업데이트를 묻게 변경
    * 업데이트를 넘어갈 수 있게 변경
- ai 번역 로그 창 스크롤 문제 해결

## 업데이트 내역 (v1.1.2)
- 테마 변경
    * 테마 색상을 보다 자연스럽게 변경
    * 그라데이션 방향을 조정하여 부드러움 수정
    * 기존 라이트 모드의 어둔운 느낌 제거
- 작품 상세보기에서 생긴 url 컬러가 변경되지 않는 문제 해결
- ai 번역기 내부 api 관련 로직 변경
    * ai 번역기 api 입력 부분을 복사/붙여넣기/전체선택만 되도록 변경
    * ai 번역기에서 기존의 api 삽입 시 바로 반영되지 않는 문제 해결
    * ai 번역기에서 api 삽입 시 자동 모델 검색 기능 추가
- 메인 아이콘 변경
- 프로그램 이름 변경
    * '다운로더' -> 'MINE DOWNLOADER - Novel(Syosetu, Kakuyomu) Downloader & Translator'
- 로드 파트 닫기 버튼 추가
- 시스템 관련
    * 기존에 폴더를 정의 하지 않을 시 생기던 폴더 문제 예외 처리
    * epub 변환시 아웃 폴더를 '환경 폴더'에서 '환경 폴더/epub'으로 변경

## 업데이트 내역 (v1.1.1)
- 태그 중복 문제 해결
- 업데이트 내역 뷰어 추가
- 인터넷 연결 확인 추가

## 업데이트 내역 (v1.1.0)
- 나로우 포인트 표시 문제 해결
- 기존 카쿠요무 파인더에서 추출 태그가 감지되지 않는 문제 해결
- 기존 추출 테그에 `서적 & 미디어화` 테그 추가
- 어플 아이콘 추가
- 자동 최신 버전 다운로드 기능 추가